{
  "name": "frogoe",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/frogoe/engine"
  },
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "check-types": "bun run --filter \"@frogoe/lint\" check-types && bun run --filter \"@frogoe/contract\" check-types && bun run --filter \"frogoe\" check-types",
    "knip": "knip",
    "prepare": "lefthook install",
    "registry:check": "bun scripts/validate-registry.mjs",
    "lint:skills": "tsx scripts/lint-skills.ts",
    "check:skill-mirror": "node scripts/check-skill-mirror.mjs",
    "check:packed-manifests": "node scripts/verify-packed-manifests.mjs",
    "check:cli-process-ownership": "node scripts/check-cli-process-ownership.mjs",
    "check:tracked-artifacts": "node scripts/check-tracked-artifacts.mjs",
    "lockstep:sync": "node scripts/sync-lockstep-versions.mjs",
    "lockstep:check": "node scripts/sync-lockstep-versions.mjs --check",
    "verify": "bun run format:check && bun run lint && bun run check-types && bun test && bun run knip && bun run registry:check && bun packages/cli/src/cli.ts lint examples/flappy && bun packages/cli/src/cli.ts lint examples/sawstorm && bun run lint:skills && node scripts/check-skill-mirror.mjs && node scripts/verify-packed-manifests.mjs && node scripts/check-cli-process-ownership.mjs && node scripts/check-tracked-artifacts.mjs"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^21.2.2",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "ajv": "^8.20.0",
    "commitlint": "^21.2.2",
    "knip": "^6.33.0",
    "lefthook": "^2.1.12",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  }
}
